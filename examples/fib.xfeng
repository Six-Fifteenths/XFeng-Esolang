# Fibonacci: (n, 0) -> (F_n, 0)
#
# 主程序流程：main 依次调用 M、F、C：
#   M: 把 n 从 L 搬到 R（L->0, R->n）。递归：if L==0 返回；否则 L-=1, R+=1, 调 M。
#   F: 斐波那契主递归。不变量：调用时 R=level，返回时 R 恢复为 level，且 L += F_level。
#      递归树遍历：每个 fib(1) 叶子给 L+1，fib(0) 叶子加 0，1-叶子总数 = F_level。
#   C: 把 R 清零（R->0）。递归：if R==0 返回；否则 R-=1, 调 C。
# 最终 L = F_n，R = 0。
#
# @F 地图（13x4）：
#   主走廊（行0）:  S ! ] ␣ . . . F ] F ) ) E
#      !(1,0): R==0 -> 下落（fib(0) 返回，走 RET0）
#      ](2,0): R-=1
#      ␣(3,0): 下落到行1，绕过 RET0 区
#      !(4,1): R==0 -> 下落（fib(1) 叶子：L+=1, R+=1，走 RET1）
#      ^(6,1): 爬回行0
#      F(7,0): 递归（R=level-1）
#      ](8,0): R-=1
#      F(9,0): 递归（R=level-2）
#      ))(10,11): R+=2 恢复 level
#      E(12,0): 返回
#   RET0（fib(0)）: 从 (1,0)! 落到 (1,1)␣ -> (1,2)␣ -> (1,3)，右行到 (7,3)^ 爬回行2 汇合
#   RET1（fib(1)）: 从 (4,1)! 落到 (4,2)，执行 ( L+=1 ) R+=1，右行
#   汇合后走行2 到 (12,2)^ -> (12,1)^ -> (12,0)E
@main
S...M...F...C...E
@M
S?[)M.E
......^
@F
S!] ...F]F))E
. ..!.^.....^
. ...().....^
.......^.....
@C
S!]C.E
.....^
