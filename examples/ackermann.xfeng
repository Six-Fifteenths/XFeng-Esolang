# 阿克曼函数：输入 (L,R)=(m,n)，输出 L=A(m,n)。
# 定义：A(0,n)=n+1；A(m,0)=A(m-1,1)；A(m,n)=A(m-1,A(m,n-1))（m,n>0）。
# 参考值：A(0,0)=1, A(1,1)=3, A(2,2)=7, A(3,2)=29, A(3,3)=61, A(3,6)=509。
#
# @A 不变量：进入 L=m,R=n -> 返回 L=m,R=A(m,n)（L 恢复进入值，结果在 R）。
#   主路径（m>0,n>0）：? ! ] A [ A ( E
#     ? 判 L，! 判 R；] R-- 后 A(m,n-1)，[ L-- 后 A(m-1,·)，( L++ 恢复。
#   L==0 分支：? 下落 -> ) R++ -> 经行1/行2/行3 绕行返回（A(0,n)=n+1）。
#   R==0 分支：! 下落 -> [ L-- -> ) R++=1 -> A(m-1,1) -> ( L++ -> 返回。
# @C：L<-R, R->0（丢弃原 L）：先递归清 L，L==0 时落到行1调 @D 复制。
# @D：L+=R, R->0（要求进入时 L=0）：每级 ( L++ 且 ] R--，递归到 R=0。
@main
S...A...C...E
@A
S?...!]A[A(E
.). . .....^
... .[)A(..^
...........^
@C
S?[C..E
.....D^
@D
S!(]D.E
......^
