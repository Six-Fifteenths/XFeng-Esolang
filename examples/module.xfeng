# 可复用模块 @C：输入 (L,R)，输出 L+R（R 清零）。
# 不变量：进入 (L,R)=(a,b) -> 返回 (L,R)=(a+b, 0)。
# 原理：! 判 R——R=0 下落返回；否则 ( L+1 且 ] R-1 后递归 C，共递归 b 层。
# 这个"累加/复制"子程序是 XFeng 的基本积木：fib / ackermann 里都有同族变体。
# 任意程序都可以像 main 这样直接调用它，把 (L,R) 换成自己的值。
@main
S...C...E
@C
S!(]C.E
......^
