
# XFeng 编程语言规范

**版本：1.0 — Formal Language Specification**

---

## 1. 语言概述

XFeng 是一种二维空间控制流编程语言。

XFeng 程序由若干张二维矩形地图组成。程序运行时，一个名为“张雪峰”的 Agent 在地图中移动。地图中的字符同时承担两类职责：

1. **执行语义**：Agent 到达某个格子后，该格子可以修改程序状态；
2. **运动语义**：Agent 在当前格子准备进行下一次移动时，由当前格子的类型决定下一步的运动方向。

XFeng 的核心数据状态由两个整数资源构成：

```math
L,R\in\mathbb Z.
```

此外，程序具有一个水平朝向变量：

```math
h\in{-1,+1}.
```

其中：

```math
h=+1
```

表示向右，

```math
h=-1
```

表示向左。

XFeng 支持函数调用。每个函数拥有独立的二维地图，并通过调用栈支持嵌套调用和递归。

XFeng 的核心执行顺序严格规定为：

```math
\boxed{\text{确定运动方向}\rightarrow\text{移动一格}\rightarrow\text{执行到达格}}
```

这是整个语言语义的基础。

---

# 2. 程序与源文件

## 2.1 源文件

一个 XFeng 源文件由若干个**程序段**组成。

每个程序段以一个声明行开始：

```text
@名称
```

主程序的声明固定为：

```text
@main
```

其他程序段是函数。

例如：

```text
@main
S...F...E
@F
S....E
@G
S...F...E
```

该文件包含：

* 一个主程序 `main`；
* 一个函数 `F`；
* 一个函数 `G`。

---

## 2.2 声明行

声明行必须满足以下格式之一：

```text
@main
```

或者：

```text
@X
```

其中 `X` 是一个合法函数名。

声明行本身不属于任何地图。

声明行必须从源文件行的第一个字符开始：

```text
@F
```

合法；

```text
  @F
```

不是声明行，而属于地图正文。

声明行末不得包含其他字符。

---

## 2.3 注释

任何**第一个字符为 `#`** 的源文件行均为注释行。

例如：

```text
# Fibonacci function
@main
S...F...E
```

注释行完全被忽略：

* 不进入任何地图；
* 不占地图高度；
* 不影响地图坐标；
* 不参与矩形性检查。

只有行首的 `#` 具有注释意义。

例如：

```text
 .#..
```

并不是注释行，而是地图内容。

---

## 2.4 空行

空行不是注释，也不是分隔符。

空行属于地图正文，因此它是长度为 0 的地图行。

由于每张地图必须是非空矩形，因此空行出现在地图正文中通常会导致：

```math
\boxed{\text{InvalidProgram}}
```

程序段之间不需要，也不允许使用特殊空行作为分隔符。

---

## 2.5 源文件编码

XFeng 源文件使用 UTF-8 编码。

字符位置按 Unicode code point 计数，而不是按字节数或视觉上的 grapheme cluster 计数。

---

# 3. 地图

## 3.1 矩形

每个程序段都对应一张独立的有限矩形地图。

设地图宽度为 (W)，高度为 (H)，则：

```math
W\ge1,\qquad H\ge1.
```

每一行必须恰有 (W) 个字符。

例如：

```text
S....
.....
..E..
```

是合法的 (5\times3) 地图。

以下地图非法：

```text
S....
...
..E..
```

因为各行长度不同。

解释器不得自动：

* 补齐短行；
* 截断长行；
* 删除首尾空格；
* 删除空白地图行；
* 修改地图尺寸。

---

# 4. 合法地图字符

XFeng 1.0 的保留地图字符为：

```text
S E . ( ) [ ] < > ^ ? !
```

此外：

```text
<ASCII space>
```

也是合法地图字符。

字符：

```text
@ #
```

属于源文件语法，不属于地图字符。

以下字符不属于 XFeng 1.0 的核心指令：

```text
+ - ~ v
```

这些字符保留为未来版本扩展所用。

---

# 5. 函数名

函数名是一个单独的 Unicode code point。

一个字符只有在同时满足以下条件时才可以作为函数名：

1. 不是 XFeng 保留字符；
2. 不是空白字符；
3. 不是 `@`；
4. 不是 `#`；
5. 具有对应的 `@函数名` 声明。

例如：

```text
@F
@A
@加
@函
```

均可作为函数声明。

地图中出现：

```text
F
加
函
```

分别表示调用对应函数。

函数名区分大小写。也就是说：

```text
@A
```

和：

```text
@a
```

是两个不同的函数。

---

# 6. 地图的 `S` 与 `E`

每张地图必须恰好包含：

```math
\boxed{1\text{ 个 }S}
```

以及：

```math
\boxed{1\text{ 个 }E}
```

`S` 和 `E` 可以位于地图中的任意位置，不要求位于角落、边界或地图两端。

---

# 7. `S`：出生点

`S` 是地图的初始位置。

从执行语义和运动语义上，`S` 与普通 `.` 完全相同：

```math
\operatorname{Exec}(S)=\operatorname{NOP}
```

并且：

```math
D(S,h,L,R)=(h,0).
```

因此，运行过程中重新移动到一个 `S` 格，不会发生任何特殊行为。

`S` 唯一的特殊性是：

> 当主程序启动，或者函数被调用时，Agent 被放置在该地图的 `S` 上。

---

# 8. `E`：终点

`E` 在地图中表示终止点。

如果 Agent 到达主程序的 `E`：

```math
\boxed{\text{程序终止}}
```

最终输出：

```math
\boxed{L}.
```

如果 Agent 到达函数的 `E`：

```math
\boxed{\text{函数返回}}
```

具体函数返回机制见第 14 节。

`E` 不产生下一次普通移动。

---

# 9. 水平朝向变量 `h`

运行时始终存在：

```math
h\in{-1,+1}.
```

其中：

```math
h=+1
```

表示向右，

```math
h=-1
```

表示向左。

`h` 是一个普通运行时变量。

它不是“当前运动方向”。

例如：

```math
h=+1
```

时，Agent 仍然可以因为当前格为空格或 `^` 而向下或向上移动。

`h` 的作用是决定所有**水平运动**应该朝哪个方向进行。

---

# 10. 初始运行状态

主程序启动时：

1. 当前地图设置为 `main`；
2. Agent 被放置在主程序的 `S`；
3. 输入资源设置为：
```math
   L=L_{\mathrm{in}},\qquad R=R_{\mathrm{in}};
```
4. 设置：
```math
   h=+1;
```
5. 不执行 `S`。

下一步按照 `S` 的正常运动语义移动。

---

# 11. 基本执行模型

XFeng 的每个 tick 严格执行：

```math
\boxed{
\text{根据当前格确定下一步方向}
\rightarrow
\text{移动一格}
\rightarrow
\text{执行到达格}
}
```

因此：

> 当前所在格决定“这一步怎么走”；移动以后，刚刚到达的格决定“这一步造成什么程序效果”。

---

## 11.1 第一步：确定运动方向

设当前地图为 (P)，当前位置为：

```math
(x,y),
```

当前格为：

```math
c=P[y][x].
```

根据：

```math
c,\quad L,\quad R,\quad h
```

确定下一移动方向：

```math
D(c,L,R,h).
```

结果必须属于：

```math
{(1,0),(-1,0),(0,1),(0,-1)}.
```

---

## 11.2 第二步：移动

若：

```math
D(c,L,R,h)=(d_x,d_y),
```

则计算：

```math
x'=x+d_x,
\qquad
y'=y+d_y.
```

如果：

```math
x'\notin[0,W-1]
```

或者：

```math
y'\notin[0,H-1],
```

则发生：

```math
\boxed{\text{BoundaryError}}
```

程序立即停止。

否则：

```math
(x,y)\gets(x',y').
```

---

## 11.3 第三步：执行到达格

读取新的当前格：

```math
c=P[y][x].
```

执行该格的执行语义。

执行可能：

* 修改 (L)；
* 修改 (R)；
* 修改 (h)；
* 调用函数；
* 返回；
* 终止；
* 或什么也不做。

---

# 12. 普通水平地面

以下格子都是水平地面：

```text
S
.
(
[
)
]
<
>
函数名
```

其共同的运动规则是：

```math
\boxed{D=(h,0)}
```

具体执行效果则分别不同。

---

# 13. 资源指令

## 13.1 `(`

到达 `(` 后：

```math
L\gets L+1.
```

下一次运动：

```math
D(\texttt{(},L,R,h)=(h,0).
```

---

## 13.2 `[`

到达 `[` 后：

```math
L\gets L-1.
```

下一次运动：

```math
D(\texttt{[},L,R,h)=(h,0).
```

---

## 13.3 `)`

到达 `)` 后：

```math
R\gets R+1.
```

下一次运动：

```math
D(\texttt{)},L,R,h)=(h,0).
```

---

## 13.4 `]`

到达 `]` 后：

```math
R\gets R-1.
```

下一次运动：

```math
D(\texttt{]},L,R,h)=(h,0).
```

---

## 13.5 资源的取值范围

(L,R) 是数学整数：

```math
\boxed{L,R\in\mathbb Z}
```

允许出现负数。

语言本身没有整数溢出。

实际解释器如果使用有限精度整数，其溢出行为属于实现限制，不属于 XFeng 语言语义。

---

# 14. 水平转向

## 14.1 `<`

到达 `<` 后：

```math
h\gets-1.
```

因此该格执行完成后，下一次运动为：

```math
(-1,0).
```

---

## 14.2 `>`

到达 `>` 后：

```math
h\gets+1.
```

因此该格执行完成后，下一次运动为：

```math
(+1,0).
```

---

# 15. 空格：自由落体

ASCII 空格  是合法地图字符。

到达空格时：

```math
\operatorname{Exec}(\texttt{ })=\operatorname{NOP}.
```

从空格进行下一次运动时：

```math
\boxed{
D(\texttt{ })=(0,1)
}
```

即直接向下移动一格。

因此自由落体不是额外的物理事件，而是空格本身定义的运动规则。

---

# 16. `^`：梯子

到达 `^` 时：

```math
\operatorname{Exec}(\texttt{^})=\operatorname{NOP}.
```

下一次运动：

```math
\boxed{
D(\texttt{^})=(0,-1)
}
```

即向上移动一格。

---

# 17. 门 `?`

`?` 是一个条件门。

到达 `?` 时：

```math
\operatorname{Exec}(?)=\operatorname{NOP}.
```

在下一次运动时检查：

```math
L=0.
```

若：

```math
L=0,
```

则：

```math
D(?)=(0,1).
```

若：

```math
L\ne0,
```

则：

```math
D(?)=(h,0).
```

因此：

```math
\boxed{
D(?)=
\begin{cases}
(0,1),&L=0,
(h,0),&L\ne0.
\end{cases}
}
```

注意：

> `?` 的条件判定不会在到达 `?` 的同一 tick 中再次移动。

必须等下一次 tick 才进行。

---

# 18. 门 `!`

`!` 是关于右手资源的条件门。

到达 `!` 时：

```math
\operatorname{Exec}(!)=\operatorname{NOP}.
```

下一次运动时检查：

```math
R=0.
```

因此：

```math
\boxed{
D(!)=
\begin{cases}
(0,1),&R=0,
(h,0),&R\ne0.
\end{cases}
}
```

---

# 19. 门的示例

地图：

```text
S?E
```

假设初始：

```math
h=+1.
```

第一 tick：

```math
S\to?
```

到达 `?` 后：

```math
\operatorname{Exec}(?)=\operatorname{NOP}.
```

第二 tick：

如果：

```math
L\ne0,
```

则：

```math
?\to E.
```

随后执行 `E`，程序终止。

如果：

```math
L=0,
```

则：

```math
D(?)=(0,1).
```

Agent 向下移动。

如果该位置越界，则产生 `BoundaryError`。

---

# 20. 函数声明

每个函数均对应一张独立地图。

例如：

```text
@main
S...A...E
@A
S...B...E
@B
S.....E
```

这里：

* `main` 是主程序；
* `A` 是函数；
* `B` 是函数。

函数地图完全独立于调用者地图。

每张函数地图必须有且仅有：

* 一个 `S`；
* 一个 `E`。

---

# 21. 函数调用

地图中出现函数名即表示函数调用控制点。

例如：

```text
S...A...E
```

当 Agent 移动到 `A` 时：

```math
\boxed{\operatorname{Call}(A)}
```

被执行。

函数调用不会把 Agent 放到函数地图中任意位置，而是：

```math
\boxed{\text{转移到函数 A 的 }S_A}
```

并开始运行函数 A。

---

# 22. 函数参数

函数显式接收两个整数资源：

```math
L,R.
```

调用函数时：

```math
(L,R)
```

原样传入。

函数内部对 (L,R) 的修改直接影响返回后的调用者状态。

函数不拥有独立的资源副本。

因此如果函数执行：

```math
L\gets L+1,
```

那么返回后调用者的 (L) 也增加一。

---

# 23. 函数继承 `h`

函数调用时，调用者当前的：

```math
h
```

直接继承给函数。

函数内部对 `<`、`>` 的操作可以改变 (h)。

这种改变不会在函数返回时撤销。

因此：

```math
(L,R,h)
\overset{f}{\longrightarrow}
(L',R',h')
```

是函数的完整运行效果。

换言之：

```math
\boxed{
\text{函数可以同时修改 }L,R,h
}
```

---

# 24. 函数入口

当调用函数 `F` 后：

```math
\text{Agent}\to S_F.
```

`S_F` 本身不执行任何操作。

由于：

```math
D(S_F)=(h,0),
```

所以函数从自己的 `S` 开始时，下一次运动总是沿**当前函数的 (h)** 水平移动。

因此，函数调用点从上、下、左、右任一方向被进入时，都不影响函数 `S_F` 的初始水平运动方向。

唯一影响因素是调用时继承的：

```math
h.
```

---

# 25. 函数可以从垂直方向被调用

这是合法的。

例如某个函数调用点 `F` 位于自由落体路径上。

Agent 可以从上方进入 `F`：

```math
\text{上方}\to F.
```

进入 `F` 后执行函数调用：

```math
F\to S_F.
```

函数从 `S_F` 开始按照继承的：

```math
h
```

水平运行。

因此函数调用点虽然可以从任意方向到达，但函数入口本身总是从水平控制流开始。

---

# 26. 函数出口 `E_F`

函数运行到达自身的 `E_F` 后：

```math
\boxed{\operatorname{RETURN}}
```

函数执行结果：

```math
(L',R',h')
```

被保留。

然后函数返回到调用者地图中的原函数调用点。

---

# 27. 函数返回点

函数返回后，Agent 重新位于原函数调用点。

例如：

```text
@main
S...F...E
```

执行过程为：

```math
S_{\rm main}
\to
F_{\rm main}
\to
S_F
\to\cdots\to
E_F
\to
F_{\rm main}
\to
E_{\rm main}.
```

返回以后：

* 不重新执行 `F`；
* 不再次调用函数；
* 下一 tick 直接从 `F` 进行运动。

由于函数调用点属于水平地面：

```math
D(F)=(h,0).
```

这里的 (h) 是**函数结束时的 (h)**。

---

# 28. 函数可以改变离开方向

例如：

```text
@main
S...F...E
@F
S...<...E
```

假设调用 `F` 时：

```math
h=+1.
```

函数内部执行 `<`：

```math
h\gets-1.
```

函数返回主程序调用点 `F`。

此时：

```math
D(F)=(-1,0).
```

因此 Agent 从 `F` 向左移动。

函数调用不要求返回方向与进入函数时相同。

---

# 29. 调用栈

函数调用需要内部调用栈以支持嵌套和递归。

假设：

```math
Main\to A\to B\to C.
```

那么当 C 返回时必须恢复到 B 的调用点；B 返回时必须恢复到 A；A 返回时必须恢复到 Main。

因此一个调用栈帧保存：

```math
\boxed{
(\text{调用者地图},\text{调用点})
}
```

调用栈**不保存**：

* (L)；
* (R)；
* (h)。

这些状态属于整个当前运行上下文，并在函数调用过程中自然延续。

---

# 30. 函数递归

函数可以调用自身。

例如：

```text
@F
S...F...E
```

是合法的。

函数也可以相互递归：

```text
@A
S...B...E
@B
S...A...E
```

函数调用栈可以无限增长。

如果递归永远不返回，则程序永远运行。

XFeng 不自动把递归判定为错误。

---

# 31. 无限循环

XFeng 不规定自动死循环检测。

如果运行产生无限状态序列：

```math
S_0\to S_1\to S_2\to\cdots,
```

而没有进入：

* 主程序 `E`；
* 函数 `E` 返回并最终终止；
* 运行时错误；

则该程序是非终止程序。

解释器可以提供最大 tick 数作为调试限制，但：

```math
\boxed{\text{max_ticks 不是 XFeng 语言语义的一部分}}
```

---

# 32. 运行时状态

XFeng 的正式运行状态定义为：

```math
\boxed{
\Sigma=
(P,x,y,h,L,R,C)
}
```

其中：

### (P)

当前地图。

### (x,y)

当前坐标。

### (h)

水平朝向：

```math
h\in{-1,+1}.
```

### (L,R)

资源：

```math
L,R\in\mathbb Z.
```

### (C)

调用栈。

---

# 33. 单步操作语义

设当前状态：

```math
\Sigma=(P,x,y,h,L,R,C).
```

设当前格：

```math
c=P[y][x].
```

首先定义运动函数：

```math
D(c,L,R,h).
```

然后：

```math
(d_x,d_y)=D(c,L,R,h).
```

计算：

```math
x'=x+d_x,
\qquad
y'=y+d_y.
```

若越界：

```math
\boxed{\Sigma\to BoundaryError}
```

否则：

```math
(x,y)\gets(x',y').
```

然后执行：

```math
\operatorname{Exec}(P[y][x],L,R,h,C).
```

因此一个普通 tick 的形式为：

```math
\boxed{
\Sigma
\xrightarrow{\text{Move}}
\Sigma'
\xrightarrow{\text{Execute}}
\Sigma''
}
```

执行阶段可以再次进入函数控制流，因此函数调用是一种控制转移，而不是普通资源修改。

---

# 34. 运动函数的完整定义

对于合法非终止格：

```math
D(c,L,R,h)
```

定义如下。

### 水平地面

若：

```math
c\in
{
S,.,(,[,),],<,>,\text{函数名}
},
```

则：

```math
\boxed{
D(c,L,R,h)=(h,0)
}
```

### 自由落体

若：

```math
c=\texttt{ },
```

则：

```math
\boxed{
D(c,L,R,h)=(0,1)
}
```

### 梯子

若：

```math
c=\texttt{\string^},
```

则：

```math
\boxed{
D(c,L,R,h)=(0,-1)
}
```

### 门 `?`

```math
\boxed{
D(?,L,R,h)=
\begin{cases}
(0,1),&L=0,
(h,0),&L\ne0.
\end{cases}
}
```

### 门 `!`

```math
\boxed{
D(!,L,R,h)=
\begin{cases}
(0,1),&R=0,
(h,0),&R\ne0.
\end{cases}
}
```

`E` 没有正常运动方向，因为到达 `E` 后立即终止或返回。

---

# 35. 执行函数的完整定义

### `S`

```math
\operatorname{Exec}(S)=NOP.
```

### `.`

```math
\operatorname{Exec}(.)=NOP.
```

### 空格

```math
\operatorname{Exec}(\texttt{ })=NOP.
```

### `^`

```math
\operatorname{Exec}(^)=NOP.
```

### `(`

```math
L\gets L+1.
```

### `[`

```math
L\gets L-1.
```

### `)`

```math
R\gets R+1.
```

### `]`

```math
R\gets R-1.
```

### `<`

```math
h\gets-1.
```

### `>`

```math
h\gets+1.
```

### `?`

```math
\operatorname{Exec}(?)=NOP.
```

### `!`

```math
\operatorname{Exec}(!)=NOP.
```

### 函数名

执行函数调用。

### 主程序 `E`

执行 `HALT`。

### 函数 `E`

执行 `RETURN`。

---

# 36. 函数调用的形式语义

当前位于：

```math
(P,p)
```

并且该格是函数 `F`。

设：

```math
P_F
```

是函数 `F` 的地图。

调用动作：

1. 将：
```math
   (P,p)
```
   压入调用栈；
2. 设置：
```math
   P\gets P_F;
```
3. 设置：
```math
   (x,y)\gets S_F;
```
4. 保持当前：
```math
   L,R,h;
```
5. 下一 tick 从 (S_F) 开始运行。

---

# 37. 函数返回的形式语义

当前位于函数 (F) 的 `E_F`。

如果调用栈栈顶为：

```math
(P_{\rm caller},p_{\rm call}),
```

则：

1. 弹出栈顶；
2. 设置：
```math
   P\gets P_{\rm caller};
```
3. 设置：
```math
   (x,y)\gets p_{\rm call};
```
4. 保持当前：
```math
   L,R,h;
```
5. 函数调用动作视为已经完成；
6. 下一 tick 从调用点移动出去。

因此：

```math
\boxed{
\text{函数返回不恢复 }h
}
```

---

# 38. 主程序终止

若主程序中的 Agent 到达 `E`：

```math
\boxed{\operatorname{HALT}(L)}
```

程序返回：

```math
L.
```

(R)、当前位置、调用栈等运行时信息不再有意义。

---

# 39. 错误分类

XFeng 将错误分为三类。

## 39.1 语法错误

源文件无法形成合法 XFeng 程序。

例如：

* 没有 `@main`；
* 多个 `@main`；
* 函数声明重复；
* 地图为空；
* 地图非矩形；
* 地图没有 `S`；
* 地图没有 `E`；
* 地图有多个 `S`；
* 地图有多个 `E`；
* 出现非法字符；
* 函数名不符合规则。

统一：

```math
\boxed{\text{InvalidProgram}}
```

---

## 39.2 未定义函数

地图中存在一个合法函数名字字符，但源文件没有对应声明：

```math
@F
```

不存在而地图中出现：

```text
F
```

则：

```math
\boxed{\text{UndefinedFunction}}
```

---

## 39.3 运行时错误

最重要的是越界：

```math
\boxed{\text{BoundaryError}}
```

例如 Agent 试图移动到：

```math
x=-1.
```

程序立即终止。

解释器不得自动：

* 反弹；
* 穿墙；
* 从另一侧出现；
* 自动改变方向。

---

# 40. 实现一致性要求

一个符合 XFeng 1.0 的解释器必须：

1. 保留地图中的所有空格；
2. 不对地图行调用 `strip()`；
3. 不删除地图内部空行；
4. 只忽略首字符为 `#` 的源文件行；
5. 将首字符为 `@` 的行解释为程序声明；
6. 严格按照“移动一格，再执行到达格”的顺序；
7. 不在到达门时立即重新移动；
8. 不在函数返回后重新执行函数调用点；
9. 保留函数对 (h,L,R) 的全部修改；
10. 使用无限精度或等价的数学整数语义实现 (L,R)。

---

# 41. 一个最小程序

```text
@main
SE
```

初始：

```math
h=+1.
```

第一步：

```math
S\to E.
```

到达 `E`。

程序输出：

```math
L_{\rm in}.
```

---

# 42. 一个资源程序

```text
@main
S(E
```

初始：

```math
(L,R)=(0,0).
```

运行：

```math
S\to(
```

到达 `(`：

```math
L\gets1.
```

然后：

```math
(\to E.
```

最终输出：

```math
1.
```

---

# 43. 一个方向程序

```text
@main
S>E
```

到达 `>` 后：

```math
h\gets+1.
```

下一步继续向右。

---

# 44. 一个函数程序

```text
@main
SFE
@F
S(E
```

初始：

```math
(L,R)=(0,0).
```

主程序：

```math
S\to F.
```

调用 `F`：

```math
F\to S_F.
```

函数：

```math
S_F\to(
```

执行：

```math
L\gets1.
```

然后：

```math
(\to E_F.
```

函数返回：

```math
E_F\to F.
```

主程序此时：

```math
L=1.
```

下一 tick：

```math
F\to E.
```

输出：

```math
\boxed{1}.
```

---

# 45. 一个函数改变 `h` 的程序

```text
@main
SFE
@F
S<E
```

假设初始：

```math
h=+1.
```

进入函数后：

```math
S_F\to<
```

执行：

```math
h\gets-1.
```

然后：

```math
<\to E_F.
```

函数返回到主程序中的 `F`。

下一步：

```math
F\to\text{左边的格子}
```

因为：

```math
h=-1.
```

函数的方向修改因此能够影响调用者。

---

# 46. Fibonacci 作为高级函数

XFeng 支持将复杂算法封装成函数。

例如，可以定义函数：

```math
F:(n,0)\mapsto(F_n,F_{n+1})
```

以及：

```math
C:(L,R)\mapsto(L,0).
```

主程序可以写成：

```text
@main
SFCE
```

其抽象计算为：

```math
(n,0)
\xrightarrow{F}
(F_n,F_{n+1})
\xrightarrow{C}
(F_n,0).
```

因此程序终止时输出：

```math
\boxed{F_n}.
```

具体的 Fibonacci 函数地图由 XFeng 核心指令实现，完整示例见仓库的 `examples/fib.xfeng`。

---

# 47. 语言的抽象结构

XFeng 可以分为四个逻辑层：

```math
\boxed{\text{地图}}
```

负责提供空间控制流。

```math
\boxed{L,R}
```

负责保存整数数据。

```math
\boxed{h}
```

负责保存水平朝向。

```math
\boxed{\text{函数调用栈}}
```

负责保存嵌套函数调用的返回位置。

因此 XFeng 并不是传统的线性指令流语言，而是一个：

```math
\boxed{
\text{二维空间控制流}
+
\text{整数资源}
+
\text{函数调用}
}
```

的确定性计算模型。

---

# 48. XFeng 1.0 核心字符速查表

| 字符   | 到达后执行   | 下一次运动               |
| ------ | ------------ | ------------------------ |
| `S`    | 无           | (h,0)                    |
| `E`    | 停机 / 返回  | 无                       |
| `.`    | 无           | (h,0)                    |
| ` `    | 无           | (0,1)                    |
| `(`    | L\gets L+1   | (h,0)                    |
| `[`    | L\gets L-1   | (h,0)                    |
| `)`    | R\gets R+1   | (h,0)                    |
| `]`    | R\gets R-1   | (h,0)                    |
| `<`    | h\gets-1     | (h,0)                    |
| `>`    | h\gets+1     | (h,0)                    |
| `^`    | 无           | (0,-1)                   |
| `?`    | 无           | L=0 时 (0,1)，否则 (h,0) |
| `!`    | 无           | R=0 时 (0,1)，否则 (h,0) |
| 函数名 | 调用函数     | 函数返回后 (h,0)         |

---

# 49. XFeng 1.0 的核心不变量

对于任何正常运行状态：

```math
\boxed{L,R\in\mathbb Z}
```

```math
\boxed{h\in{-1,+1}}
```

```math
\boxed{(x,y)\text{ 永远位于当前地图内部}}
```

并且每一个非终止、非错误状态都有唯一的下一步行为。

因此 XFeng 的运行关系是确定性的：

```math
\boxed{
\Sigma\rightarrow\Sigma'
}
```

而不是：

```math
\Sigma\rightarrow{\Sigma_1,\Sigma_2,\ldots}.
```

---

# 50. 规范性声明

XFeng 实现必须以本规范定义的语义为准，而不得根据字符的自然语言名称、视觉形状或“物理直觉”自行增加规则。

特别是以下行为均不属于 XFeng 1.0：

* 自动重力系统；
* 自动碰撞反弹；
* 自动落地修正；
* 地图边界环绕；
* 自动死循环检测；
* 隐式资源复制；
* 隐式资源溢出；
* 函数返回后恢复旧的 (h)；
* 函数返回后重新执行调用点。

XFeng 中每一次移动、每一次资源修改、每一次函数调用和每一次函数返回均由上述规则唯一确定。

因此，对于给定的：

```math
\boxed{
\text{源文件}+\text{初始 }(L,R)
}
```

XFeng 解释器应产生唯一的运行轨迹，直至：

```math
\boxed{\text{HALT}}
```

或：

```math
\boxed{\text{RuntimeError}}
```

或无限运行。
